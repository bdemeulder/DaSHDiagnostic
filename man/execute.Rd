% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execute.R
\name{execute}
\alias{execute}
\title{Execute the DaSHDiagnostic study pipeline}
\usage{
execute(
  connectionDetails,
  cdmDatabaseSchema,
  vocabularyDatabaseSchema,
  cohortDatabaseSchema,
  cohortTable,
  databaseId,
  outputFolder,
  incremental,
  includeCohortStats,
  minCellCount = 5,
  runCreateCohorts = TRUE,
  runBaselineCharacteristics = TRUE,
  runIRandTTEAnalysis = TRUE
)
}
\arguments{
\item{connectionDetails}{DatabaseConnector connection details object.}

\item{cdmDatabaseSchema}{Schema containing the OMOP CDM tables (read-only).}

\item{vocabularyDatabaseSchema}{Schema containing the vocabulary tables (usually same as CDM).}

\item{cohortDatabaseSchema}{Schema where cohort tables are written (needs read/write/delete).}

\item{cohortTable}{Name of the cohort table to create.}

\item{databaseId}{Short site identifier used to label all outputs.}

\item{outputFolder}{Path where all result files are written.}

\item{incremental}{Logical. Passed to \code{generateStudyCohorts}: if
\code{TRUE}, skip cohorts whose SQL checksum is unchanged since the last run.}

\item{includeCohortStats}{Logical. Passed to \code{generateStudyCohorts}: if
\code{TRUE}, rebuild cohort SQL with inclusion-rule statistics and export them.}

\item{minCellCount}{Minimum number of events/subjects reported; smaller cells
are suppressed. Default 5.}

\item{runCreateCohorts}{Logical. Run cohort generation. Default \code{TRUE}.}

\item{runBaselineCharacteristics}{Logical. Run baseline characterization. Default \code{TRUE}.}

\item{runIRandTTEAnalysis}{Logical. Run the IR/TTE safety analysis. Default \code{TRUE}.}
}
\value{
Invisibly \code{NULL}; called for its side effects (files written to
  \code{outputFolder}).
}
\description{
Runs the study end to end: cohort generation, baseline characteristics, and the
incidence-rate / time-to-event safety analysis. Each stage can be toggled independently.
}
